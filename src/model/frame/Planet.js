var
    geo    = require('geometry'),
    ccp    = geo.ccp,
    Parent = require('../abstract/Parent');

function Planet() {
    Planet.superclass.constructor.call(this);
}

Planet.inherit(Parent, {
	
});